interface:
  display_name: "Build Backend"
  short_description: "Build layered backend services and APIs"
  default_prompt: "Use $build-backend to implement the selected backend scope and return a verified handoff."
